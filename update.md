### v3.1.3(260828)
- 修复 `设置`页面背景不一致的问题
- 新增 `音量增强幅度`（默认增强0mdB）
- 新增 `环线`（实验性），可在`路线`中设置
- 优化 在地图上编辑路线时，可以添加相同的站点（适配`环线`）
- 优化 在地图上编辑路线时，可以点击`站点列表`来移动或删除`站点`



### v3.1.2(260827)
- 新增 `手动切换站点时报站`在`语言和播报`中（默认关闭）
- 修复 部分站点名称显示时，不读取`stationLangTable.json`的问题
- 修复 `上一站`按钮切换错误的问题
- 修复 部分格式音频无法正常播报的问题


### v3.1.1(260818)
- 新增 `全站路线`自定义显示站点类型
- 新增 `沿途站点示意`的轨迹线
- 优化 `路线`列表上下滚动异常的问题
- 优化 `主控`启用`路线纠偏`时轨迹不连续的问题
- 优化 `Android SDK` 更新到 `37`
- 优化 `主控`显示效果
- 优化 `沿途站点示意`点击站点的效果
- 修复 部分动态刷新率手机文字滚动异常的问题


### v3.0.9(260608)

- 优化 还原 `站点`/`路线` 数据时，检查文件正确性
- 优化 `在线搜索路线` 路线名称的获取方式
- 修复 `电显` 文字滚动忽慢的问题
- 修复 `在线搜索路线` 列表显示错乱的问题
- 修复 `添加站点` 显示删除按钮的问题
- 修复 开启 `路线贴合道路` 时终点站轨迹颜色不正确的问题
- 修复 短时间内多次报站偶现的崩溃问题

### v3.0.8(260423)

- 新增 `添加站点` 时可以获取当前位置
- 新增 自定义 `即将进站` `出站` 半径范围
- 新增 自动切换到 `即将进站` `进站` `出站` 状态开关
- 修复 `主控` 的 `路线运行信息` 重复显示路线名称的问题
- 优化 更友好的 `半径范围` 编辑方式
- 优化 更新 `预设数据` 的站点和路线（260423）
- 优化 更新 `预设播报格式` （260423）

### v3.0.7(260412)

- 修复 部分文本显示错误
- 修复 `路线`点击站点显示信息及播报不一致的问题
- 新增 `line`音频文件夹，播报`<line>`标签时优先使用

### v3.0.6(260210)

- 修复 `搜索站点`失效的问题
- 优化 定位启用时查看地图的体验

### v3.0.5(251114)

- 修复 部分设备打开外部链接失败的问题
- 优化 `自动报站间隔` 选取范围

### v3.0.4(251113)

- 新增 `巡航模式`
- 新增 `站点` 删除
- 新增 `设置` `自动播报间隔`

### v3.0.3(251029)

- 新增 长按报站按钮`模拟运行报站`
- 新增 `主控` `停止播报`按钮

- 修复 播报格式校验异常的问题
- 修复 部分设备路线站点列表滚动速度异常的问题
- 修复 播报不同声道的音频异常的问题
- 修复 不同刷新率下站点滚动速度不一致的问题

- 优化 多行随机播报的选取算法
- 优化 媒体焦点的获取逻辑
- 优化 `主控`界面布局

### v3.0.2(251016)

- 新增 支持横屏（实验性）
- 新增 多行随机播报（格式可写多行，播报时随机选择一行）

- 优化 获取定位权限后自动开启`主控`定位
- 优化 路线标点及文字颜色视觉效果
- 优化 `stationLangTable.json`现在位于目录`Announcer`
- 优化 统一了路线列表的排序规则

- 修复 在地图编辑路线时轨迹线丢失的问题
- 修复 连续弹出`stationLangTable.json文件不存在`的问题
- 修复 播报结束后仍压低其他应用播放音量的问题
- 修复 点击`查找到站点`闪退的问题

### v3.0.1(251010)

- 新增 `点击地图中断播报`开关
- 新增 设置`搜索城市`定位获取
- 新增 更方便的路线添加模式
- 新增 `检查更新`
- 新增 `路线贴合道路`（替换原有的`线路轨迹纠偏（实验性）`）
- 新增 `路线`选项卡点击首/末站切换上下行
- 新增 `站点` `路线`选项卡快速滚动

- 修复 部分安卓版本下状态栏沉浸异常
- 修复 设置`从上行终点站出站时切换下行`点击无反应
- 修复 自动备份文件名不一致

- 优化 设置中的一些文本描述
- 优化 移除旧版设置
- 优化 移除旧版设置
- 优化 路线轨迹区分进站/出站
- 优化 路线/站点滚动列表视觉效果
- 优化 站点列表性能

### TODO

- 修复
    - 在系统权限管理手动关闭权限后，dialog跳转无效的问题
    - 部分设备无法播放有损格式音频（如ogg）的问题
    - `电显`内容带`R`属性时，持续播放时长不正确的问题
    - 概率后台CPU占用异常的问题

- 新增
    - 自定义英文字体
    - 适配环线
    - 自定义主题色
    - 预测性返回手势适配（等待各厂商ROM适配）
    - 路线站点列表滚动速度自定义
    - 路线站点列表高度（n个字）自定义

- 优化
    - 适配English（长期）
    - 桂林公交报站库补全（长期）
